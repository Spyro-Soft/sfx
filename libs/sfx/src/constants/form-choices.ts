export enum FRAMEWORKS {
  react = 'react',
  angular = 'angular',
  nextJs = 'next',
}

export enum BUNDLERS {
  vite = 'vite',
  webpack = 'webpack',
}

export enum ERROR_MONITORING_OPTIONS {
  yes = 'yes',
  no = 'no',
}

export enum UPDATE_ACTIONS {
  migrateNx = 'Update Nx version and other dependencies',
  updateConfig = 'Update additional configuration',
}

export enum REPOSITORY_PLATFORMS {
  github = 'Github',
  gitlab = 'Gitlab',
  // bitbucket = 'Bitbucket',
  azure = 'Azure',
}
